**Add your own guidelines here**
<!-- Brand Guidelines — “Porto Demo 33” Inspired (for Figma) Base reference: Porto - Homepage 33 :contentReference[oaicite:0]{index=0} # 1) General guidelines * Build with a clean, commerce-first layout: strong header/navigation, clear product grids, promotional banners, and trust badges (shipping/returns/support/payment). :contentReference[oaicite:1]{index=1} * Prioritize scannability: short headings, compact filters, strong price hierarchy, and consistent spacing. * Prefer simple shapes: mostly rectangular cards, subtle rounding, light borders, soft shadows (sparingly). * Use color for meaning (actions, promos, badges), not decoration. * Keep pages “white/light” with high contrast and small, controlled accent areas. # 2) Foundations ## 2.1 Color system (tokens) Porto supports “Primary / Secondary” (and extended colors) as global theme colors. :contentReference[oaicite:2]{index=2} Use that same idea in Figma: define roles first, then map hex values. ### Brand palette (recommended, grocery/organic vibe) (These are suggested starting values; adjust after sampling your final UI screenshots.) * Primary / Brand (Green): #2F8F3A * Primary Hover: #25752F * Secondary / Accent (Orange): #FF8A00 * Secondary Hover: #E57800 * Tertiary (Leaf tint / highlight): #6CC04A ### Neutrals * Text / High: #1F2328 * Text / Medium: #5B616A * Text / Muted: #8A9199 * Border / Hairline: #E6E8EB * Surface / Card: #FFFFFF * Surface / Subtle: #F7F8FA * Background: #FFFFFF ### Semantic * Success: #2F8F3A * Warning: #FFB020 * Error: #D92D20 * Info: #2E90FA ### Commerce-specific * Price / Current: Text/High * Price / Old (strikethrough): Text/Muted * Discount badge bg: Secondary * “New” badge bg: #111827 ### Color usage rules * One primary CTA per section; other actions should be secondary/tertiary. * Links default to Primary; hover to Primary Hover. * Sale/discount uses Secondary (orange) for maximum visibility. * Borders are neutral; do not tint borders with brand colors unless it’s a focused/active state. ## 2.2 Typography Aim for a modern, highly readable eCommerce stack. * Font family (recommended): Inter (fallback: system UI) * Numerals: tabular lining (for prices if available) * Base font size: 14px (desktop commerce standard) * Line-height: * Body: 1.5 * Headings: 1.2–1.3 ### Type scale (desktop) * H1: 36/44, Semibold * H2: 28/36, Semibold * H3: 22/30, Semibold * H4: 18/26, Semibold * Body: 14/21, Regular * Small: 12/18, Regular * Micro/Label: 11/16, Medium (UPPERCASE optional for badges) ### Text rules * Product titles: 1–2 lines max (truncate with ellipsis). * Price: always more prominent than rating/reviews. * Use sentence case for UI labels; ALL CAPS only for tiny badges (“NEW”, “SALE”). ## 2.3 Spacing & layout ### Spacing tokens (8pt system) * 4, 8, 12, 16, 24, 32, 40, 48 ### Radius * Inputs/Buttons: 6px * Cards: 10px * Badges: 999px (pill) or 6px (tag) ### Elevation (sparingly) * Card default: 0–1 (border only preferred) * Hover: subtle shadow + lift 2–4px * Modals: stronger shadow + dim overlay ### Grid * Desktop container: 1200–1280px max * Columns: 12 * Gutter: 24px * Product grids: * Desktop: 4–6 columns (category dependent) * Tablet: 2–3 columns * Mobile: 2 columns (or 1 for dense detail) ## 2.4 Iconography * Style: outline icons, 1.5–2px stroke, rounded joins * Sizes: 16 (inline), 20 (buttons), 24 (feature tiles), 32 (empty states) * Keep icons neutral; use brand color only for “active/selected” states. ## 2.5 Imagery * Product images: clean background, consistent aspect ratio (1:1 preferred) * Promo banners: bold headline + short subhead + one CTA * Avoid busy textures; keep backgrounds light and airy. # 3) Components ## 3.1 Buttons ### Primary * Bg: Primary * Text: White * Radius: 6px * Height: 40px (desktop), 44px (mobile) * Hover: Primary Hover * Disabled: bg Border/Hairline, text Muted ### Secondary (outline) * Border: Primary * Text: Primary * Bg: transparent * Hover: subtle Primary tint background (5–8%) ### Tertiary (text) * Text: Primary * No border * Hover: underline or subtle tint background ### Button content rules * Labels are verbs (“Add to cart”, “View all”, “Subscribe”). * If icon + text: 8px gap; icon left. ## 3.2 Links * Default: Primary * Hover: Primary Hover + underline * In dense tables/filters: reduce emphasis (no underline until hover) ## 3.3 Inputs * Height: 40px (desktop), 44px (mobile) * Border: Hairline * Focus: 2px outline in Primary (or Primary at 30% opacity) * Error: Error color border + helper text ## 3.4 Badges & labels * NEW: dark background, white text * SALE / -XX%: Secondary background, white text * Use compact pill badges on product cards; never more than 2 badges at once. ## 3.5 Product Card (core commerce pattern) Must include: * Image * Title (1–2 lines) * Price (current + optional old price) Optional: * Rating + count * Badges (NEW/SALE) * Quick actions: wishlist/compare (icons), quickview (secondary) Rules: * Primary CTA (“Add to cart”) appears on hover (desktop) or always visible (mobile) depending on density. * Keep card padding 12–16px. * Use border over shadow by default. ## 3.6 Navigation (header) Inspired structure: utility bar + main header + department/mega menu. :contentReference[oaicite:3]{index=3} Rules: * Utility bar: small text, muted, includes shipping promo and account links. * Main header: logo, search (prominent), cart (icon + count). * Department menu: strong category list; mega menus use imagery sparingly. ## 3.7 Promo modules * “Feature tiles” (Shipping/Returns/Support/Payment) are short, icon + title + one line body. :contentReference[oaicite:4]{index=4} * Use neutral backgrounds; icons can be Primary. ## 3.8 Sections * Section heading: H3 + short subtext * “View All” is a secondary action aligned right ## 3.9 Footer * Multi-column links + newsletter sign-up + social icons. :contentReference[oaicite:5]{index=5} * Keep footer background darker neutral or light-neutral; ensure contrast. # 4) States & interactions * Hover (desktop): increase contrast, subtle elevation, show quick actions. * Active/selected: Primary border + subtle tint background. * Loading: skeletons for product cards and banners (avoid spinners for grids). * Empty states: icon + short message + one CTA. # 5) Content & tone (microcopy) * Friendly, simple, commerce-direct. * Prefer “Free shipping on orders over $99” style trust copy. :contentReference[oaicite:6]{index=6} * Avoid long paragraphs; 1–2 lines per supporting text. # 6) Figma setup checklist * Create Color Styles by role (Primary, Secondary, Text/High, Border, Surface, etc.) before picking final hex. * Create Text Styles for: H1/H2/H3/H4, Body, Small, Label. * Create Components with Variants: * Button (Primary/Secondary/Tertiary) × (Default/Hover/Disabled) × (Icon/no icon) * Input (Default/Focus/Error/Disabled) * Badge (New/Sale/Percent) * Product Card (Default/Hover/Sale/New) * Header (Default/Sticky) and Footer -->
